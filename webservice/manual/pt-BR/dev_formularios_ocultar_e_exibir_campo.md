# Formulário: Ocultar e Exibir Campo

Utilize o método `set_visivel([campo], [bool])` para definir a visibilidade de um campo. Exemplo:

```
set_visivel(txtCPF, true); // exibe o campo
set_visivel(txtCNPJ, false); // esconde o campo
```