on: 
  push:
    branches:
      - 24@8.2
name: 🚀 Deploy on push versão 24 PHP 8.2
jobs:
  web-deploy:
    name: 🎉 Deploy v24 PHP 8.2
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files PortalHomolog
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_PORTALHOMOLOG }}
        password: ${{ secrets.FTP_PASSWORD_PORTALPRODHOMOLOG }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy PortalHomolog
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_HOMOLOG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi
    
    - name: 📂 Sync files PortalProd
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_PORTALPROD }}
        password: ${{ secrets.FTP_PASSWORD_PORTALPRODHOMOLOG }}
        server-dir: ./

    - name: 🔗 Call API after deploy PortalProd
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_PROD }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi
    
    - name: 📂 Sync files 000205 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000205 }}
        password: ${{ secrets.FTP_PASSWORD_000205 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000205 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000205_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000205
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000205 }}
        password: ${{ secrets.FTP_PASSWORD_000205 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000205
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000205 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000347 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000347 }}
        password: ${{ secrets.FTP_PASSWORD_000347 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000347 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000347_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000347
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000347 }}
        password: ${{ secrets.FTP_PASSWORD_000347 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000347
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000347 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi
        
    - name: 📂 Sync files 000348 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000348 }}
        password: ${{ secrets.FTP_PASSWORD_000348 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000348 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000348_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000348
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000348 }}
        password: ${{ secrets.FTP_PASSWORD_000348 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000348
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000348 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000353 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000353 }}
        password: ${{ secrets.FTP_PASSWORD_000353 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000353 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000353_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000353
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000353 }}
        password: ${{ secrets.FTP_PASSWORD_000353 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000353
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000353 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000333 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000333 }}
        password: ${{ secrets.FTP_PASSWORD_000333 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000333 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000333_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000333
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000333 }}
        password: ${{ secrets.FTP_PASSWORD_000333 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000333
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000333 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000335 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000335 }}
        password: ${{ secrets.FTP_PASSWORD_000335 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000335 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000335_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000335
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000335 }}
        password: ${{ secrets.FTP_PASSWORD_000335 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000335
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000335 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi
        
    - name: 📂 Sync files 000226 Hlg
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000226 }}
        password: ${{ secrets.FTP_PASSWORD_000226 }}
        server-dir: ./homolog/

    - name: 🔗 Call API after deploy 000226 Hlg
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000226_HLG }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi

    - name: 📂 Sync files 000226
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_HOST_PORTALPRODHOMOLOG }}
        username: ${{ secrets.FTP_USERNAME_000226 }}
        password: ${{ secrets.FTP_PASSWORD_000226 }}
        server-dir: ./

    - name: 🔗 Call API after deploy 000226
      run: |
        RESPONSE_CODE=$(curl -X POST "${{ secrets.FTP_URL_DBU_000226 }}${{ secrets.SUFIXO_DBU_API }}" \
          -H "Content-Type: application/json" \
          -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
          -d '{"status": "success", "message": "FTP deploy complete"}' \
          -w "%{http_code}" -o response.txt -s)
        
        if [ "$RESPONSE_CODE" -ne 200 ]; then
          echo "❌ API call failed with status code $RESPONSE_CODE"
          cat response.txt
          exit 1
        fi