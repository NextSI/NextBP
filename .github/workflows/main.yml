on: 
  push:
    branches:
      - 24@8.2

name: 🚀 Deploy on push versão 24 PHP 8.2

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - nome: PortalHomolog
            username: FTP_USERNAME_PORTALHOMOLOG
            password: FTP_PASSWORD_PORTALHOMOLOG
            dir: ./homolog/
            api: FTP_URL_DBU_HOMOLOG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: PortalProd
            username: FTP_USERNAME_PORTALPROD
            password: FTP_PASSWORD_PORTALPROD
            dir: ./
            api: FTP_URL_DBU_PROD
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000205 Homolog
            username: FTP_USERNAME_000205
            password: FTP_PASSWORD_000205
            dir: ./homolog/
            api: FTP_URL_DBU_000205_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000205 Prod
            username: FTP_USERNAME_000205
            password: FTP_PASSWORD_000205
            dir: ./
            api: FTP_URL_DBU_000205
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000347 Homolog
            username: FTP_USERNAME_000347
            password: FTP_PASSWORD_000347
            dir: ./homolog/
            api: FTP_URL_DBU_000347_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000347 Prod
            username: FTP_USERNAME_000347
            password: FTP_PASSWORD_000347
            dir: ./
            api: FTP_URL_DBU_000347
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000348 Homolog
            username: FTP_USERNAME_000348
            password: FTP_PASSWORD_000348
            dir: ./homolog/
            api: FTP_URL_DBU_000348_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000348 Prod
            username: FTP_USERNAME_000348
            password: FTP_PASSWORD_000348
            dir: ./
            api: FTP_URL_DBU_000348
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000353 Homolog
            username: FTP_USERNAME_000353
            password: FTP_PASSWORD_000353
            dir: ./homolog/
            api: FTP_URL_DBU_000353_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000353 Prod
            username: FTP_USERNAME_000353
            password: FTP_PASSWORD_000353
            dir: ./
            api: FTP_URL_DBU_000353
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000333 Homolog
            username: FTP_USERNAME_000333
            password: FTP_PASSWORD_000333
            dir: ./homolog/
            api: FTP_URL_DBU_000333_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000333 Prod
            username: FTP_USERNAME_000333
            password: FTP_PASSWORD_000333
            dir: ./
            api: FTP_URL_DBU_000333
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000335 Homolog
            username: FTP_USERNAME_000335
            password: FTP_PASSWORD_000335
            dir: ./homolog/
            api: FTP_URL_DBU_000335_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000335 Prod
            username: FTP_USERNAME_000335
            password: FTP_PASSWORD_000335
            dir: ./
            api: FTP_URL_DBU_000335
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000226 Homolog
            username: FTP_USERNAME_000226
            password: FTP_PASSWORD_000226
            dir: ./homolog/
            api: FTP_URL_DBU_000226_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000226 Prod
            username: FTP_USERNAME_000226
            password: FTP_PASSWORD_000226
            dir: ./
            api: FTP_URL_DBU_000226
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000234 Homolog
            username: FTP_USERNAME_000234_HLG
            password: FTP_PASSWORD_000234_HLG
            dir: ./
            api: FTP_URL_DBU_000234_HLG
            server: FTP_HOST_PORTALPRODHOMOLOG

          - nome: 000234 Prod
            username: FTP_USERNAME_000234
            password: FTP_PASSWORD_000234
            dir: ./
            api: FTP_URL_DBU_000234
            server: FTP_HOST_PORTALPRODHOMOLOG

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files ${{ matrix.nome }}
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets[matrix.server] }}
          username: ${{ secrets[matrix.username_secret] }}
          password: ${{ secrets[matrix.password_secret] }}
          server-dir: ${{ matrix.dir }}

      - name: 🔗 Call API after deploy ${{ matrix.nome }}
        run: |
          echo "📡 Chamando API para ${{ matrix.nome }}"

          RESPONSE_CODE=$(curl -X POST "${{ secrets[matrix.api] }}${{ secrets.SUFIXO_DBU_API }}" \
            -H "Content-Type: application/json" \
            -H "Dbutoken: ${{ secrets.DBU_API_TOKEN }}" \
            -d '{"status": "success", "message": "FTP deploy complete"}' \
            -w "%{http_code}" -o response.txt -s)

          echo "🔍 HTTP Response Code: $RESPONSE_CODE"

          RESPONSE_BODY=$(cat response.txt)
          echo "📄 API Response:"
          echo "$RESPONSE_BODY"

          if [ "$RESPONSE_CODE" -ne 200 ]; then
            echo "❌ API call failed with status code $RESPONSE_CODE"
            exit 1
          fi

          if [[ "$RESPONSE_BODY" != *"Atualizacao de estrutura procedimentos e funcoes do banco de dados concluida"* ]]; then
            echo "❌ Resposta inesperada da API!"
            echo "🔎 Esperado: 'Atualizacao de estrutura procedimentos e funcoes do banco de dados concluida'"
            exit 1
          fi

          echo "✅ Deploy e atualização do banco de dados realizados com sucesso para ${{ matrix.nome }}"
