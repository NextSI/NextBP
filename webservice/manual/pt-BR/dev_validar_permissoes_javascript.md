# Validar Permissões em JavaScript
```javascript
if (App.verifica_permissao(App.temp.empresa, 'exemplo_permissao_a')) {
    // possui a permissão 
}
```

Veja também como [Criar Permissões](?i=pt-BR&p=dev_permissoes)